# Kenney Fantasy UI Borders

Source: https://kenney.nl/assets/fantasy-ui-borders
License: CC0 1.0 (public domain — see `LICENSE.txt`, no attribution required,
kept anyway for provenance)

Only `panel-013.png` is used (of the pack's 140 assets) — a rounded-square
panel shape, tinted at runtime via `StyleBoxTexture.modulate_color` in
`assets/ui/betalife_theme.tres` rather than pre-colored, so the same source
texture serves every UI tint the theme needs.
